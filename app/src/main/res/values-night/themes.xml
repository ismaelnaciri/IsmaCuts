<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.IsmaCuts" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorBackground">@color/dark_background</item>
        <item name="textColor">@color/dark_text_color</item>
        <item name="professionalBackground">@color/light_background</item>
        <item name="logoGeneral">@drawable/barber_logo_white_1</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splas_background</item>
        <item name="logoGeneral">@drawable/barber_logo_white_1</item>
    </style>
</resources>